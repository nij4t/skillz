{
  "name": "skillz",
  "version": "0.1.0",
  "description": "Displays radar chart with languages",
  "scripts": {
    "build": "parcel build public/index.html --public-url /skillz",
    "deploy": "git --work-tree='./dist' add . && git commit -m 'Generate artifacts' && git push origin gh-pages"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "@types/chart.js": "^2.9.19",
    "chart.js": "^2.9.3",
    "parcel": "^1.12.4"
  }
}
